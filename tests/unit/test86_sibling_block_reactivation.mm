$( The same variable name may be declared in sibling blocks, since each
   declaration becomes inactive when its own block closes. $)
$c wff $.
${
  $v x $.
  fx1 $f wff x $.
$}
${
  $v x $.
  fx2 $f wff x $.
$}
